name: watt
description: A new Flutter package project.
version: 0.1.0
homepage:

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  file_picker: ^5.1.0
  flutter:
    sdk: flutter
  http: ^0.13.4
  intl: ^0.17.0
  witt: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
