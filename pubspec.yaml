name: watt
description: Flutter prebuilt widgets and utils to make development faster
version: 1.6.0
repository: https://github.com/weaze-id/watt

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  file_picker: ^5.3.0
  flutter:
    sdk: flutter
  http: ^0.13.6
  intl: ^0.18.1
  witt: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
